# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: docling-service
  namespace: docling
spec:
  selector:
    app: docling-serve
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5001
  type: ClusterIP
